{
  "name": "kingsmen-seeding",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check worker/index.js && node build.mjs --out .build-check && rm -rf .build-check"
  },
  "devDependencies": {
    "@babel/standalone": "^7.26.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.0"
  }
}
