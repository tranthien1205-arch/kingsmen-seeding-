{
  "name": "kingsmen-content-os",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev.mjs",
    "test": "node --test --no-warnings \"tests/*.test.mjs\"",
    "check": "node --check worker/index.js && node build.mjs --out .build-check && node -e \"require('fs').rmSync('.build-check',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@babel/standalone": "^7.26.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwindcss": "^3.4.0"
  }
}
